const config = {
    // Discord Token
    token: "your discord bot token",
    command: "?create",
    // Btw you can cutom the commands like !create , $create , etc.

    // Embed Settings
    embed: {
        color: "#5865F2", //https://htmlcolorcodes.com/color-picker/
        footer: "source code of nexttectzx bot - dev : jn03" //Btw you can change but pls give me an credit :D
    },

    // Perms Can change the perms if wish
    perms: "MANAGE_ROLES" 
    //Dont change anything
}

module.exports = config
