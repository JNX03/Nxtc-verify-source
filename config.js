const config = {
    // Discord Token
    token: "your discord bot token",
    command: "?createautorole",

    // Embed Settings
    embed: {
        color: "#5865F2",
        footer: "source code of nexttectzx bot - dev : jn03"
    },

    // Perms Can change the perms if wish
    perms: "MANAGE_ROLES"
}

module.exports = config
